node_modules/
dist/
dist-types/
*.tsbuildinfo
data/
.DS_Store
.wrangler/

# secretos de dev local del worker (Client Secret de Discord, etc.)
.dev.vars
.dev.vars.*

# symlinks por agente (los reales viven en .agents/skills/)
.claude/skills/

# hojas fuente de sprites (24 MB): se re-recortan con tools/slice-*; los PNG
# optimizados de public/sprites SI se versionan
apps/client/assets/sprites/

# worktrees temporales de agentes
.claude/worktrees/
